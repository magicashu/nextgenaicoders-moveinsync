# LangGraph4j adapter gate

Implement `WorkflowEngine` here only after the D-028 spike proves conditional routing, bounded fan-out/fan-in, serializable state, checkpointed approval resume and nested OpenTelemetry spans. The deterministic adapter remains the fallback.
