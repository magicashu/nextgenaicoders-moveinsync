# Test taxonomy

- `unit`: formulas and state transitions
- `integration`: DuckDB, PostgreSQL and model adapters
- `contract`: OpenAPI and JSON schemas
- `golden`: G1-G3 trajectories
- `security`: tenant isolation and excessive agency
- `architecture`: package-boundary rules

The first executable unit test is under the production package it covers.
