# AWS deployment placeholder

Keep the hackathon deployment minimal: one containerized Spring Boot service, one static React build behind a web server/CDN, managed PostgreSQL for control state, and OpenTelemetry export. Add concrete IaC only for resources the team actually deploys.
