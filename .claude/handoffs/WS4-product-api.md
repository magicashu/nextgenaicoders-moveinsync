# Handoff — WS4 Product API

Owned paths: `backend/src/main/java/.../reporting/**`, `conversation/**`, `api/**`, `backend/src/test/java/.../{reporting,conversation,api}/**`.
Branch and commit: `feat/product-api` @ `ea12891` (base `90c1683`).
Contract/version consumed: OpenAPI 0.1.0 (`/api/v1/demo/brief`), `decision-brief.schema.json`, frozen records `DecisionBrief`, `EvidenceBundle`, `MetricResult`, `ActionProposal`, `ApprovalRequest/Decision`, `ExecutionReceipt`, `AuditEvent`, ports `AuditSink`, `AccessAuthorizer`, `WorkflowCoordinator.createDemoBrief`.
Feature demonstrated (endpoints): `GET /api/v1/briefs/morning`, `POST /api/v1/workflows`, `GET /api/v1/workflows/{id}`, `POST /api/v1/questions`, `GET /api/v1/approvals/{id}`, `POST /api/v1/approvals/{id}/decision`, `GET /api/v1/audit/{id}`, `GET /api/v1/demo/brief`. One evidence bundle -> operations section (headline KPI with configured-target label, supporting KPIs, findings with evidence ids, caveats, action, approval preview with scope/evidence version/timestamp/consequence, receipt) and leadership section (narrative, forwardable text) with a non-divergence guard; trust panel (versions, latency, model/tool use, capability gaps, transitions); contextual questions with deterministic intent allowlist and typed refusals; stable error envelope.
Tests and exact result: `./mvnw -q -pl backend test` -> exit 0 (6 classes, 23 tests): `ProductApiTest` 8 (WebMvc slice), `ContextualQuestionServiceTest` 3, `OpenApiProviderTest` 2. Live on the scaffold seam: smoke suite from WS6 passes in 4 s (isolation 404, missing header 400, unknown tenant 403, refusals, approval bypass 400/404, duplicate decision 409).
Trace spans added: none in the API (trace ids surface from `RunView.traceId` in `trust.traceId` and `AuditResponse.traceId`); error envelopes carry a fresh `traceId` for support.
Known failure/fallback: scaffold gateway serves M01 only and performs no side effect on approval (receipt APPROVED_NOT_EXECUTED with explanation); dependency failures map to 503 with no fabricated number; cross-tenant reads are 404 so runs cannot be enumerated.
Shared change requested: (1) OpenAPI 0.2.0 describing the six endpoints, `ApiDtos` shapes and identity headers (`X-Actor-Id`, `X-Business-Unit`, `X-Roles`) - draft shapes in `api/dto/ApiDtos.java` and `frontend/src/core/contracts.ts`; (2) `decision-brief.schema.json` status enum add `REPORT_ONLY`; (3) replace `AllowlistActorResolver` with an adapter over WS3 `RegistryIdentityResolver` (`mobility.api.actor-resolver=governance`) and `ScaffoldDecisionRunGateway`/`ScaffoldPortBeans` with an adapter over WS2 `WorkflowCoordinator`/`ResumableWorkflowEngine.find` and WS3 `ApprovalLifecycle` (`mobility.api.gateway=workflow`); (4) CORS/dev proxy: Vite proxies `/api` to 8080, no CORS config added.
Decision-register update required: yes - record the identity-header convention for the demo and the 404-for-foreign-tenant rule.
Integration steps: merge after WS2/WS3; implement `DecisionRunGateway` in `config/**` mapping `WorkflowRun` -> `RunView` (fields are 1:1: claims, capabilityGaps, dataQualityNotes, branchStatus, evidenceVersion, verification, approvalRequest, receipt, transitions, modelUsage, versions); run `ProductApiTest` unchanged and `scripts/demo/smoke.sh`.
