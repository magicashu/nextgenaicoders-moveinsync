# Handoff — WS1 Governed Analytics

Owned paths: `backend/src/main/java/.../ingestion/**`, `metrics/**`, `anomaly/**`, `backend/src/main/resources/sql/**`, `backend/src/test/java/.../{ingestion,metrics,anomaly}/**`, `data/fixtures/**`, `data/corrupted/**`.
Branch and commit: `feat/governed-analytics` @ `54abda6` (rebased onto Integration Owner commit `8cd84c8`; the `DuckDbMetricService` conflict with Codex's date-parsing patch was resolved by keeping the seven-file implementation, which already handles both date formats, and adding a `DuckDbMetricService(MobilityDataProperties)` convenience constructor so `OfficialDatasetReconciliationTest` (D-042) compiles and passes).
Contract/version consumed: `MetricQuery/MetricResult/MetricId/MetricUnit/MetricStatus` (metrics-v1.1, D-039), `TenantContext`, `MobilityDataProperties`, `AnomalyService.assess` and `MetricService.query/delayedTripRate` kept source-compatible for the scaffold workflow.
Feature demonstrated: seven-file discovery/validation/normalisation into DuckDB (column-tolerant, `NA` nulls, dedupe by composite keys, checksums and `data-<hash>` version, Q1-Q14 quality report); all 18 governed metrics from reviewed SQL through a token-only parameter renderer with the allowlisted dimension set; capability matrix derived from data; contribution rankings with 300/500 minimum-volume qualification, delay-reason and alert-type distributions, daily snapshots, cross-tenant peers; deterministic anomaly detection (impact, confidence components, priority) and the G3 data-regime classifier; seven worker contribution tools.
Tests and exact result: `JAVA_HOME=$(/usr/libexec/java_home -v 21) ./mvnw -q -pl backend test` -> exit 0 on the rebased branch; 15 classes, 49 tests, 0 failures, including Codex's `OfficialDatasetReconciliationTest` (4,357/19,913, 21.88/12.28/9.60) and `MobilityCapabilitiesIntegrationTest` (SCAFFOLD mode, as expected on this branch). Official gates (`OfficialDatasetGateTest`, 12 tests, 8.2 s) pass with the canonical dataset: 4,357/19,913 = 21.88 vs 12.28; Clearwater 24.07 / 51.1% share; shift 10:30 47.38; 14 qualified vendors all rose (17.15-28.37); DRIVER 39.3 vs 33.4; 7,780 rider legs; 6,753 collisions; 708/72 dedupes; 158 negative vanta-Sea lines, medians 1,390.34 and 1,020.00; 136 capped / 20 quarantined delays (Pooja 77/4); marshal zero 473,692; sign-off step-down week 2026-05-18; catalyst-Sac healthy. Live demo endpoint on the official data: cold load 9.7 s, then ~50 ms per brief.
Trace spans added: none (analytics is synchronous); every result carries `source`, `dataVersion`, `contractVersion`, evidence ids for rankings/distributions.
Known failure/fallback: missing files or columns degrade to typed `UNSUPPORTED` results (V1-V5 covered by `CorruptedVariantTest`); the legacy four-column sample still yields M01 30/10 with a below-minimum-volume caveat; official-data tests skip when the directory is absent (`MOBILITY_OFFICIAL_DATA_DIR` overrides).
Shared change requested: (1) `MetricService` port gains sibling ports `CapabilityMatrixService`, `ContributionService`, `MetricSnapshotService`, `AnomalyDetectionService` and `WorkerContributionTools` in WS1 packages - Codex should reference them from the WS2 `AnalyticsGateway` adapter; (2) optional property `mobility.analytics.*` was not added, loading is in-memory per process (10 s on official data) - consider a persistent DuckDB file path property in `config/**`; (3) `scripts/data/validate-fixture.py` and `scripts/demo/verify-api.sh` expectations on the tiny fixture are unchanged.
Decision-register update required: yes - D-042 proposal: fixture 3 and the G1/G2 illustrative M04/M05 late-pickup shares in the profile document were computed over all legs (nulls in the denominator); under contract v1.1 the values are G1 25.77% vs 21.29% (late), G2 final week 11.82% vs 4.09% (May), late drops 29.17%. Also record: `NA` literal null token in alerts, `trip_nodal` `NA`, and that `pinnacle-Slc` has zero Sev-1/2 alerts (M15 unsupported for that tenant).
Integration steps: merge branch; run `./mvnw -pl backend test` in the canonical checkout so `OfficialDatasetGateTest` executes; wire `DeterministicAnomalyDetectionService`, `WorkerContributionTools`, `DuckDbCapabilityMatrixService`, `DuckDbContributionService` into the WS2 `AnalyticsGateway` adapter (1:1 field mapping, see `INTEGRATION-NOTES.md`); set `MOBILITY_DATA_DIR` to the official directory for demos.
