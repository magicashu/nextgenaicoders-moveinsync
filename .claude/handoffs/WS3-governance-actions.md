# Handoff — WS3 Governance and Actions

Owned paths: `backend/src/main/java/.../access/**`, `approval/**`, `action/**`, `audit/**`, `backend/src/main/resources/db/migration/**`, `backend/src/main/resources/application-postgres.yml`, `backend/src/test/java/.../{access,approval,action,audit}/**`.
Branch and commit: `feat/governance-actions` @ `86d8def` (base `90c1683`).
Contract/version consumed: D-041 ports implemented without signature changes: `AccessAuthorizer` (`RoleBasedAccessAuthorizer`), `ApprovalStore` (via `ApprovalRepository`), `ActionRevalidator` (`DeterministicActionRevalidator`), `ActionExecutor` (`IdempotentActionExecutor`), `AuditSink` (via `AuditLedger`), `WorkflowCheckpointStore` (`InMemory`/`JdbcWorkflowCheckpointStore`).
Feature demonstrated: trusted identity resolution against the tenant registry and role allowlist; role-based, fail-closed authorization; approval lifecycle approve/reject/edit/expire with edit constraints; PostgreSQL and in-memory repositories; optimistic-version checkpoints over Flyway V1; V2 migration (approval_request, decision edits, execution receipts, audit hash columns, append-only trigger); revalidation of authorization, approval state, evidence version, expiry and action state; exactly-one mock effect across duplicates, concurrency and processes with four mock adapters; hash-chained audit ledger.
Tests and exact result: default `./mvnw -q -pl backend test` -> exit 0 (7 classes, 22 tests). PostgreSQL integration `MOBILITY_TEST_POSTGRES_URL=jdbc:postgresql://localhost:55432/mobility MOBILITY_TEST_POSTGRES_USER=mobility ./mvnw -q -Ppostgres -pl backend test -Dtest=PostgresControlPlaneIntegrationTest` -> exit 0, 4 tests (optimistic checkpoints + restart, race-safe decisions, 12 concurrent executions across two executor instances = 1 effect, UPDATE/DELETE on audit_event rejected by trigger). Branch boots with in-memory adapters.
Trace spans added: none; audit events `APPROVAL_REQUESTED/APPROVE/REJECT/EDIT/EXPIRED`, `ACTION_EFFECT_RECORDED`, `ACTION_EFFECT_FAILED`, `ACTION_DUPLICATE_SUPPRESSED` carry actor, action, evidence version and timestamps; every event has `traceId` and chain hashes.
Known failure/fallback: PostgreSQL unavailable -> in-memory adapters (explicitly not production); adapter failure -> APPROVED_NOT_EXECUTED, retry allowed up to 3 attempts only because mock adapters produce no effect when they throw; duplicate approval for the same action rejected by unique partial index.
Shared change requested: (1) migration order V1 (Codex) then V2 (WS3); if V1 changes, V2's ALTERs must follow; (2) `application-postgres.yml` now sets `mobility.workflow.control-plane=postgres`; Codex's composition should honour that property when choosing adapters and remove WS2's `InMemoryControlPlaneBeans`; (3) `ApprovalRepository` (extends `ApprovalStore`) and `AuditLedger` (extends `AuditSink`) are the beans to inject - consider promoting `findByActionId/findRecord` and `chainForRun/verifyChain` into the frozen ports; (4) WS4 should use `RegistryIdentityResolver`/`TrustedIdentity` instead of its allowlist resolver once merged.
Decision-register update required: yes - record the hash-chained append-only audit design, the retry rule for APPROVED_NOT_EXECUTED, and the tenant/role registry as the identity source for the demo (gateway claims in production).
Integration steps: merge after WS1; apply Flyway on the demo database (`-Ppostgres`, profile `postgres`); recovery assumption: on restart the engine reloads `workflow_checkpoint` (step AWAITING_APPROVAL) and `approval_request`, recomputes evidence and executes at most once under `run:action` idempotency keys; verify with WS2 `ApprovalResumeTest` re-pointed at the JDBC beans.
