# Handoff — WS5 React Experience

Owned paths: `frontend/**`.
Branch and commit: `feat/react-experience` @ `7bed4c7` (base `90c1683`).
Contract/version consumed: `ApiDtos` shapes from WS4 mirrored in `src/core/contracts.ts` (MorningBriefResponse, QuestionRequest/Response, ApprovalView, ApprovalDecisionRequest/Response, AuditResponse, TrustPanel, ApiError) plus the frozen `DecisionBrief`, `EvidenceBundle`, `MetricResult`, `ActionProposal`, `ExecutionReceipt`, `AuditEvent`; identity headers `X-Actor-Id`/`X-Business-Unit`/`X-Roles`.
Feature demonstrated (routes are state tabs at `/`): Morning brief (hero, confidence, KPI cards with configured-target label, impact tiles, findings with evidence chips, caveats, recommended action with approval summary, leadership summary with copy-to-forward, suggested questions); Investigation (seven branches with capability greying, findings by worker, full evidence table); Approval (scope, evidence version and timestamp, consequence, expiry, approve/reject/edit with comment, EXECUTED/REJECTED/EXPIRED/APPROVED_NOT_EXECUTED results); Audit (append-only timeline, trace id, optional Langfuse link via `VITE_LANGFUSE_URL`, hidden reasoning keys); Trust (versions, latency, model calls/tokens, tool calls, capability gaps, node trace); Ask drawer (typed refusals, evidence-linked answers, follow-ups); loading, healthy, anomaly (G1), degraded (G2 with unsupported KPI and caveats), unavailable (503) and error states; responsive at 860 px.
Tests and exact result: `npx tsc -b` -> ok; `npm test` -> 1 file, 7 tests passed; `npm run build` -> built (258 kB JS, 9.8 kB CSS). Mock (`VITE_USE_MOCKS=true`) and HTTP clients share one `CopilotApi` interface with identical payload types.
Trace spans added: none (browser); trace ids displayed from API payloads.
Known failure/fallback: without a backend the shell shows the dependency-unavailable panel; `VITE_USE_MOCKS=true` runs the full judge flow on typed fixtures; expiry is displayed from backend status only (demo replays historical as-of dates).
Shared change requested: OpenAPI 0.2.0 for the six endpoints (same as WS4); confirm `REPORT_ONLY` brief status; keep `frontend/package.json` dependencies unchanged (no additions were made).
Decision-register update required: no new decision; note that the UI never computes metrics or expiry.
Integration steps: merge after WS4; run `npm ci && npm run build && npm test`; start Vite (`npm run dev`) with the backend on 8080 and walk the five-minute flow: brief -> KPI evidence -> investigation -> approve -> audit -> trust -> ask; screenshots to capture: hero+KPIs, evidence drawer, approval preview, executed receipt, audit timeline, trust panel.
