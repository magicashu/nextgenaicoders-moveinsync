# Handoff — WS2 Agent Workflow

Owned paths: `backend/src/main/java/.../workflow/**`, `evidence/**`, `backend/src/main/resources/prompts/**`, `backend/src/test/java/.../{workflow,evidence}/**`.
Branch and commit: `feat/agent-workflow` @ `04a2ec9` (base `90c1683`).
Contract/version consumed: D-041 ports (`AccessAuthorizer`, `WorkflowCheckpointStore`, `ApprovalStore`, `ActionRevalidator`, `ActionExecutor`, `AuditSink`), `WorkflowState/WorkflowOutcome/WorkflowEngine`, `WorkflowProperties` (4 steps, 1 correction, 12 tool calls, 10 s, 30 min), `ActionProposal/ApprovalRequest/ApprovalDecision/ExecutionReceipt`, `DecisionBrief`, `EvidenceBundle/EvidenceItem`, `MetricResult`.
Feature demonstrated: deterministic 18-node state machine with the four-node investigation subgraph (parallel, isolated, budgeted); Supervisor/Investigator/Critic/Briefing roles deterministic-first with bounded validated model assist; evidence package with content-hash version; deterministic verifier (uncited numbers, causal wording, single-vendor blame); one correction cycle; healthy, report-only, approval-wait, executed, rejected, expired, not-executed terminal states; checkpointed pause and resume with revalidation and exactly one effect; facilities-head-only cross-tenant peers; contextual question intent restriction; transition events for telemetry.
Tests and exact result: `./mvnw -q -pl backend test` -> exit 0; 6 classes, 30 tests, 0 failures (`G1TrajectoryTest` 3, `FailurePathTest` 8, `ApprovalResumeTest` 9, plus baseline). Branch boots; scaffold demo endpoint returns REPORT_ONLY on the tiny fixture (confidence below 0.40 because six analyses are unsupported on the scaffold seam).
Trace spans added: `TransitionEvent` per node plus `investigation.<worker>.<subnode>` events via `TransitionListener` (stable span names = `WorkflowNode.spanName()`); `ModelUsage` per role call; `RUN_SUMMARY` audit payload with tool calls, correction cycles, model calls, elapsed ms, data and evidence versions.
Known failure/fallback: no model configured -> deterministic roles (accounted as fallbacks); worker failure/timeout -> PARTIAL/FAILED branch with caveat and lower confidence; tool-budget exhaustion -> SKIPPED branches; adapter failure -> APPROVED_NOT_EXECUTED; stale evidence/expired/cross-tenant approver -> no execution. LangGraph4j spike (1.8.26) passed all five gates but is DROPPED for the hackathon (`workflow/adapter/langgraph4j/README.md`).
Shared change requested: (1) `decision-brief.schema.json` status enum add `REPORT_ONLY` (and optionally `FAILED`); (2) `WorkflowStep` gained additive constants HEALTHY, REPORT_ONLY, EXECUTED, APPROVED_NOT_EXECUTED, REJECTED, EXPIRED; (3) `WorkflowProperties` could gain `maxOutputTokens` and `runDeadline` (currently constants 600-900 tokens per role, deadline = toolTimeout x steps); (4) add Spring AI to default dependencies and implement `LanguageModelPort` via `ChatClient` in `config/**` (property `mobility.workflow.language-model=<provider>`); (5) `InMemoryControlPlaneBeans` and `ScaffoldAnalyticsGateway` are scaffolding to remove when WS3/WS1 adapters are wired (`mobility.workflow.control-plane=postgres`, `mobility.workflow.analytics-gateway=governed`); (6) `AllowedDimensions` duplicates WS1 `Dimension` - unify at integration.
Decision-register update required: yes - record the LangGraph4j DROP verdict (D-028 gate result), the REPORT_ONLY brief status and the resume-by-recomputation design (evidence freshness proven by recomputing detection, or replaying nodes 3-11 after restart).
Integration steps: merge after WS1 and WS3; implement `AnalyticsGateway` adapter over WS1 (`detect` <- `DeterministicAnomalyDetectionService.detect`, `runWorker` <- `WorkerContributionTools.run`, `capabilities` <- `CapabilityMatrix.statuses`, `crossTenantPeers` <- `ContributionService.crossTenantDelayedTripRate`); bind WS3 beans to the frozen ports and delete `InMemoryControlPlaneBeans`; wire `TransitionListener` to WS6 `TraceRecorder`; run `G1TrajectoryTest` unchanged plus the real-data G1 through `WorkflowCoordinator.run`.
