# Handoff — WS6 Quality, Security and Telemetry

Owned paths: `backend/src/main/java/.../observability/**`, `backend/src/test/java/.../{quality,security,architecture}/**` (plus `observability` tests), `backend/src/test/resources/fixtures/quality/**`, `evals/**`, `scripts/demo/**`, `.github/workflows/**`.
Branch and commit: `feat/quality-telemetry` @ `401ee3e` (base `90c1683`).
Contract/version consumed: frozen records for artifact shapes (`EvidenceBundle/EvidenceItem`, `ActionProposal`, `ActionExecutionCommand`, `TenantContext/ActorContext`), `contracts/schemas/*` required fields, WS4 `ApiDtos` shapes (read by `scripts/demo/scorecard.sh` and `smoke.sh`), WS2 `TransitionListener` (to be wired), Langfuse OTLP contract (`/api/public/otel/v1/traces`, Basic auth, `x-langfuse-ingestion-version: 4`, `langfuse.*`/`gen_ai.*` attributes; verified against Langfuse docs 2026-09-05).
Feature demonstrated: one nested root trace per run (`TraceRecorder`/`Span`, trace id = run id) covering request, authorization, roles, tools, critic, report, approval, revalidation, execution and audit link; `Redaction` (secrets, emails, phones, JDBC passwords, prompt/completion keys dropped, rider ids tokenized); `CostAndLatencyLedger` (model calls, tokens, estimated cost, tool latency); `LangfuseOtlpExporter` non-blocking with degraded status; deterministic evaluators (schema, evidence support, tenant scope, transitions, idempotency) with clean and tampered artifacts; golden G1/G2/G3 cases, ten metric fixtures, 11 adversarial and 8 recovery cases, V1-V5 generator; package-boundary and no-SQL-in-workflow/controller tests; smoke, verify-api, scorecard and variant scripts with runtime budgets; CI build (12 min), evaluation (15 min) and release (20 min) workflows.
Tests and exact result: `./mvnw -q -pl backend test` -> exit 0 (11 classes, 27 tests, 3 skipped by design until live artifacts exist). Against the WS4 branch API: `scripts/demo/smoke.sh` -> `SMOKE OK in 4 s`; `scripts/demo/scorecard.sh` -> exit 0, gates G1 PASS, G3 PASS, SEC PASS, AUDIT PASS, G2 SKIPPED (scaffold seam has no vanta-Aus data), zero-tolerance counters all 0, p50 17 ms, max 72 ms. Langfuse outage test: 5 traces finished in < 200 ms with an unreachable endpoint, exporter degraded, local trace ids preserved.
Trace spans added: `mobility_run` root; `authorize_scope`, `supervisor_plan`, `run_investigations` > `tool.<worker>`, `evidence_critic`, `compose_decision_brief`, `approval_interrupt`, `revalidate`, `execute`, `audit_link`, `model.<role>`; attributes from `TraceAttributes` only.
Known failure/fallback: Langfuse unavailable -> in-memory/degraded export, product unaffected; scorecard gates SKIP when an endpoint returns non-200 and FAIL only on wrong behaviour; ArchUnit not available, so boundary rules are enforced by import scanning (`PackageBoundaryTest`).
Shared change requested: (1) wire WS2 `TransitionListener` -> `TraceRecorder.Trace.begin/end` and WS2 `ModelUsage` -> `Trace.recordModelCall` in `config/**`; expose `TraceExporter.status()` and `Trace.summary()` in the API trust panel; (2) add `LANGFUSE_HOST/LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY/MOBILITY_ENVIRONMENT` to `.env.example`; (3) optional root dependencies ArchUnit and opentelemetry-exporter-otlp if preferred over the hand-written OTLP JSON; (4) `.gitignore` entry `evals/results/*` except `.gitkeep`; (5) `scripts/demo/verify-api.sh` now accepts REPORT_ONLY/HEALTHY and official-data values (21.88/12.28).
Decision-register update required: yes - record deterministic-evaluator-first evaluation with LLM judges limited to explanation quality, the hand-written OTLP/JSON exporter decision, and the runtime budgets (CI 8/12 min, evaluation 15 min, smoke 60 s).
Integration steps: merge last; wire telemetry as above; run `scripts/demo/scorecard.sh` against the integrated API on the official dataset, then `./mvnw -pl backend test` so `MetricFixtureGateTest`, `ScorecardTest` and `AdversarialCorpusTest` evaluate the produced artifacts; write `evals/results/metric-results.json` from WS1 fixtures (ids F01-F10) to enable the live fixture comparison; bookmark one Langfuse trace and the matching audit chain for the demo.
